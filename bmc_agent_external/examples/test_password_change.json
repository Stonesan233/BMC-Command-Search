{
  "suite_name": "BMC Password Change Smoke Test",
  "test_cases": [
    {
      "id": "TC_USER_PASSWORD_CHANGE_001",
      "title": "Change testuser password via interactive ipmcset",
      "target_host": "192.168.1.100",
      "steps": [
        "Health check: verify testuser exists and is not locked",
        "Change password from $ENV{OLD_PASSWORD} to $ENV{NEW_PASSWORD} using ipmcset -t user -d password -v testuser",
        "Verify the new password works by querying user state",
        "Health check: verify testuser is still enabled after password change"
      ]
    }
  ]
}
